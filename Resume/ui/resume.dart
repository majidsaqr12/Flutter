import 'package:flutter/material.dart';

import 'package:resume/ui/scaffold.dart';

class resume extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: scaffold(),
    );
  }
}
